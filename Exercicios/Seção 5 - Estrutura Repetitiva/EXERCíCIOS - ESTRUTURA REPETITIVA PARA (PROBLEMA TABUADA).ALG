Algoritmo "EXERCíCIOS - ESTRUTURA REPETITIVA -PARA- (PROBLEMA TABUADA)"

Var

   N, x, i: inteiro

Inicio

   escreva("Deseja a tabuada para qual número?: ")
   leia(N)
   
   para i de 1 até 10 faca
      x <- N * i
      escreval(N," X", i, " =", x)
   fimpara


Fimalgoritmo