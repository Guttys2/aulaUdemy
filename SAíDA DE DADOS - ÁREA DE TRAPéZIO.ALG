Algoritmo "Saída de Dados - Área de Trapézio"

Var


   BaseMenor, BaseMaior, h, area : real

Inicio


   BaseMenor <- 6.0
   BaseMaior <- 8.0
   h <- 5.0
   
   area <- (BaseMenor + BaseMaior) / 2.0 * h
   
   escreval(area)

Fimalgoritmo