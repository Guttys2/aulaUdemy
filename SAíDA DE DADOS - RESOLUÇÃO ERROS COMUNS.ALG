Algoritmo "Saída de Dados - Área de Trapézio"

Var


   a : real
   b : inteiro
   c : inteiro
   x : inteiro

Inicio

   x <- 2
   a <- 5.0
   b <- int(a) + 10
   c <- b \ x

   escreval(b)
   escreval(c)

Fimalgoritmo